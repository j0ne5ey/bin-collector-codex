{
  "name": "border-bin-day",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A mobile-first Scottish Borders bin collection PWA.",
  "scripts": {
    "test": "node --test",
    "fetch": "node scripts/fetch-collections.mjs",
    "serve": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  },
  "engines": {
    "node": ">=20"
  }
}
